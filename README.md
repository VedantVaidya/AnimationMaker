# Animation Maker

A browser-based tool for creating illustrated animated videos.

## Setup & Run

1.  **Install Dependencies**:
    ```bash
    npm install
    ```

2.  **Start Application**:
    ```bash
    npm run dev
    ```

3.  **Build**:
    ```bash
    npm run build
    ```
